version: "3"
services:
    backend:
        build: "./server"
        # volumes:
        #     - "./server:/home/codex/server"
    frontend:
        build:
            context: "./client"
    webserver:
        build:
            context: "./webserver"
        ports:
            - "3000:3000"
